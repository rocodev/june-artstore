<div class="row">
   <div class="col-md-12">
 
    <h2> 購物車 </h2>
 
    <table class="table table-bordered">
      <thead>
        <tr>
          <th class="col-md-1">商品資訊</th>
          <th class="col-md-2"> 商品名稱 </th>
          <th>單價</th>
          <th>  </th>
        </tr>
      </thead>
      <tbody>
        <% current_cart.cart_items.each do |item| %>
        <tr>
          <td><%= image_tag item.product.image_url(:mini).to_s %></td>
 
            <td> 
              
 
              <%= link_to(item.product.title, admin_product_path(item.product)) %>
            </td>
            <td> <%= item.product.price %> </td>
            <td> 
              <%= link_to  cart_item_path(item), :method => :delete do %>
              <i class="glyphicon glyphicon-trash"></i>
              <% end %>
            </td>
          </tr>
          <% end %>
 
        </tbody>
      </table>
 
      <div class="total group">
        <span class="pull-right">
           <span> 總計 <%= render_cart_total_price(current_cart) %> NTD  
        </span>
      </div>
  
      <hr>
      <div class="checkout">
          <%= link_to("確認結賬", checkout_carts_path, :method => :post , :class => "btn btn-primary btn-lg btn-danger pull-right") %>
      </div>
 
 
    </div>
 
 
  </div>