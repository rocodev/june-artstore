<div class="row orderlist col-md-10 col-md-offset-1">
  <h2>訂單列表 </h2>
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>訂單序號</th>
        <th>訂單時間</th>
      </tr>
    </thead>
    <tbody>
      <% @orders.each do |order| %>
      <tr>
        <td> <%= link_to order.token, order_path(order.token) %> </td>
        <td class="timeAgo"> <%= time_ago_in_words(order.created_at) %> 之前 </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>