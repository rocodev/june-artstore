<div class="col-md-12">

<div class="group">
  <%= link_to "New", new_admin_product_path, class: "btn btn-large btn-primary pull-right" %>
</div>

<table class="table table-striped table-hover">
		<thead> <tr>
			<td>#</td>
			<td>品名</td>
			<td>說明</td>
			<td>數量</td>
			<td>action</td>
		</tr>
	</thead>

	<tbody id="products_container">

		<% @products.each do |product| %>
			<tr>
				<td><%= link_to product.id, edit_admin_product_path(product) %></td>
				<td><%= link_to product.title, edit_admin_product_path(product) %></td>
				<td><%= product.quantity %></td>
				<td><%= product.price %></td>
				<td>
				<%= link_to "Edit", edit_admin_product_path(product) %> |
				<%= link_to "Delete", admin_product_path(product), :method => :delete, data: { confirm: "Are you sure?" } %> |
				Show</td>
			</tr>
		<% end %>
	</tbody>
	</table>

</div>